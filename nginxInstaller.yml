---
  - name: nginxInstaller
    hosts: all
    gather_facts: yes
    become: yes
    become_user: root
    tasks:
      - name: Check if nginx is installed. If not, install it! Latest Version.
        yum:
          name: nginx
          state: latest